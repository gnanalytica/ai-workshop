export interface PreviewCohortOption {
  id: string;
  name: string;
  slug: string;
  status: string;
}

export interface PreviewUserOption {
  id: string;
  full_name: string | null;
  email: string;
}
