import { PageStub } from "@/components/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Faculty handbook"
      description="Operating handbook and pre-training material."
      caps={["schedule.read"]}
    />
  );
}
