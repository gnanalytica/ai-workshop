import { PageStub } from "@/components/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Certificate"
      description="Your completion certificate (unlocks at graduation)."
      caps={[]}
    />
  );
}
