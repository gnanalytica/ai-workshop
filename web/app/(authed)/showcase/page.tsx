import { PageStub } from "@/components/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Showcase"
      description="Public-ready capstones from past and current cohorts."
      caps={[]}
    />
  );
}
