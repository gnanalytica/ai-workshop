import { PageStub } from "@/components/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Teams"
      description="Form a team or join one for the capstone."
      caps={[]}
    />
  );
}
